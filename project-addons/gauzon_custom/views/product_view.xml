<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_search_form_view_add_supp_codes" model="ir.ui.view">
        <field name="name">product.search.form_add_supp_codes</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_search_form_view"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="supplier_codes"/>
            </field>
        </field>
    </record>

    <record model="ir.actions.act_window" id="action_purchase_line_product_product_tree">
        <field name="context">{}</field>
        <field name="domain">[('product_id','in',active_ids), ('state', 'in', ['purchase', 'done'])]</field>
        <field name="name">Purchases</field>
        <field name="res_model">purchase.order.line</field>
        <field name="view_id" ref="purchase.purchase_order_line_tree"/>
    </record>

    <record id="view_product_product_purchase_buttons_from" model="ir.ui.view">
        <field name="name">product.product.purchase.button.inherit</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view"/>
        <field name="groups_id" eval="[(4, ref('purchase.group_purchase_user'))]"/>
        <field name="arch" type="xml">
            <button name="toggle_active" position="before">
               <button class="oe_stat_button" name="%(action_purchase_line_product_product_tree)d"
                   type="action" icon="fa-shopping-cart">
                   <field string="Purchases" name="purchase_count" widget="statinfo"/>
               </button>
            </button>
        </field>
    </record>

    <menuitem id="mrp.menu_mrp_product_form"
            action="product.product_normal_action"
            parent="mrp.menu_mrp_bom"/>

</odoo>
