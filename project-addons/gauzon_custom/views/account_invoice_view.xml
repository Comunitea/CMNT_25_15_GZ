<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="invoice_tree_add_ref" model="ir.ui.view">
        <field name="name">account.invoice.tree_add_ref</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_tree"/>
        <field name="arch" type="xml">
            <field name="reference" position="replace">
                <field name="reference"/>
            </field>
        </field>
    </record>

    <record id="invoice_supplier_tree_add_date" model="ir.ui.view">
        <field name="name">account.invoice.supplier.tree.add_date</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_supplier_tree"/>
        <field name="arch" type="xml">
            <field name="date_invoice" position="after">
                <field name="date"/>
            </field>
        </field>
    </record>

</odoo>
