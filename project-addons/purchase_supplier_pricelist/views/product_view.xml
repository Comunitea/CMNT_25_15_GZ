<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="product_supplierinfo_form_view_add_amounts" model="ir.ui.view">
            <field name="name">product.supplierinfo.form.view.add_amounts</field>
            <field name="model">product.supplierinfo</field>
            <field name="inherit_id" ref="product.product_supplierinfo_form_view"/>
            <field name="arch" type="xml">
                <xpath expr="//label[@for='price']" position="before">
                    <field name="gross_amount"/>
                    <field name="discount"/>
                </xpath>
            </field>
        </record>

        <record id="product_supplierinfo_tree_view_add_amounts" model="ir.ui.view">
            <field name="name">product.supplierinfo.tree.view</field>
            <field name="model">product.supplierinfo</field>
            <field name="inherit_id" ref="product.product_supplierinfo_tree_view"/>
            <field name="arch" type="xml">
                <field name="min_qty" position="after">
                    <field name="gross_amount"/>
                    <field name="discount"/>
                </field>
            </field>
        </record>

</odoo>
